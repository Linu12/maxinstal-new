name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 85.215.134.251
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
           cd /var/www/maxinstal
            git pull
            docker-compose down
            docker-compose build --no-cache
            docker-compose up -d